<UserControl x:Class="AIDemoUI.Views.SamplesImportView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:views="clr-namespace:AIDemoUI.Views"
             xmlns:sd="clr-namespace:AIDemoUI.SampleData"
             xmlns:vm="clr-namespace:AIDemoUI.ViewModels"
             xmlns:conv="clr-namespace:AIDemoUI.Converters"
             mc:Ignorable="d" 
             d:DesignHeight="225" d:DesignWidth="400">

    <d:UserControl.DataContext>
        <sd:SamplesImportVMSampleData/>
    </d:UserControl.DataContext>

    <Grid>

        <!-- Import Samples Button -->

        <Border BorderBrush="LightGray" BorderThickness="1">
            <StackPanel>
                <Button MinHeight="50" VerticalAlignment="Stretch" HorizontalAlignment="Stretch" FontSize="20"
                        Content="Import Samples"
                        Command="{Binding ImportSamplesCommand}" 
                        CommandParameter="{Binding RelativeSource={RelativeSource Mode=Self}}"/>
                <TextBlock Text="{Binding SampleImportWindow.DataContext.SelectedTemplate}" VerticalAlignment="Center" HorizontalAlignment="Center" Margin="0,10,0,5"/>
            </StackPanel>
        </Border>

    </Grid>
</UserControl>
